{
  "name": "tachyon-core",
  "version": "0.1.0",
  "napi": {
    "binaryName": "napi",
    "packageName": "tachyon-core"
  },
  "dependencies": {
    "@node-rs/helper": "^1.6.0"
  },
  "scripts": {
    "deps": "npm install",
    "build:ffi": "cargo build --release --manifest-path src/lib/Cargo.toml",
    "build:napi": "napi build --release --platform --no-dts-header --manifest-path src/napi/Cargo.toml && npm run cleanup",
    "build": "npm run build:napi && npm run build:ffi",
    "cleanup": "mv src/napi/napi.*.node ./"
  },
  "devDependencies": {
    "@napi-rs/cli": "3.5.0"
  }
}
